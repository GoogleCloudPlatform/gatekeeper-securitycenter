apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    control-plane: controller-manager
    gatekeeper-securitycenter/operation: manager
    gatekeeper-securitycenter/system: "yes"
  name: gatekeeper-securitycenter-controller-manager
  namespace: gatekeeper-securitycenter
spec:
  replicas: 1
  selector:
    matchLabels:
      control-plane: controller-manager
      gatekeeper-securitycenter/operation: controller
      gatekeeper-securitycenter/system: "yes"
  template:
    metadata:
      annotations:
        container.seccomp.security.alpha.kubernetes.io/manager: runtime/default
      labels:
        control-plane: controller-manager
        gatekeeper-securitycenter/operation: controller
        gatekeeper-securitycenter/system: "yes"
    spec:
      containers:
      - args:
        - findings
        - manager
        - --source=$(SOURCE)
        - --cluster=$(CLUSTER)
        - --interval=120
        - --dry-run=false
        env:
        - name: SOURCE
          valueFrom:
            configMapKeyRef:
              key: SOURCE_NAME
              name: gatekeeper-securitycenter-config
        - name: CLUSTER
          valueFrom:
            configMapKeyRef:
              key: CLUSTER_NAME
              name: gatekeeper-securitycenter-config
        - name: DEBUG
          value: "false"
        image: ghcr.io/googlecloudplatform/gatekeeper-securitycenter:v0.4.1@sha256:cc05b05b62d45deb960429a70db81a0d8718086e2f292efb54ea3b4e4ff5a4cb
        name: manager
        resources:
          requests:
            cpu: 250m
            memory: 512Mi
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - all
          readOnlyRootFilesystem: true
          runAsGroup: 65532
          runAsNonRoot: true
          runAsUser: 65532
      serviceAccountName: gatekeeper-securitycenter-controller
